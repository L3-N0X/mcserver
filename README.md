# MCSERVER

Attach to Server with command:

```bash
docker compose attach {container_name}
```

Quit with `CTRL + P` `CTRL + Q`

## Hetzner dedicated server

### Install Image

```bash
installimage
```

- Follow the prompts to install the image.
- Select `Ubuntu 22.04 LTS` as the operating system.
